{
  "name": "devisor-deamon",
  "version": "1.0.0",
  "description": "",
  "main": "index.ts",
  "scripts": {
    "built": "rimraf dist && npx tsc",
    "prebuilt": "npm run built",
    "start": "node dist/server.js",
    "serve": "nodemon --watch . --ext ts --exec \"tsc && node dist/server.js\""
  },
  "author": "",
  "license": "Apache-2.0",
  "dependencies": {
    "@types/express": "^5.0.0",
    "@types/node": "^22.10.5",
    "concurrently": "^9.1.2",
    "devisor-deamon": "file:",
    "ejs": "^3.1.10",
    "express": "^4.21.2",
    "rimraf": "^6.0.1",
    "typescript": "^5.7.2"
  },
  "devDependencies": {
    "nodemon": "^3.1.9"
  }
}
